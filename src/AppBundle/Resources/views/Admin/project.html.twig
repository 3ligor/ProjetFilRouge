{% extends "AppBundle::layout.html.twig" %}

{% block content %}
	{% include "AppBundle:Admin:menuAdmin.html.twig" with {'default' : 'project'} %}
	<div class="row">
		<table class="table table-condensed table-hover">
			<th>Type</th>
			<th>Intitulé</th>
			<th class="text-center">Date de création</th>
			<th class="text-center">Date de début</th>
			<th class="text-center">Date de fin</th>
			<th class="text-center">Validation</th>
				{% for project in projects %}
				<tr>
					<td>
						{% for categorie in project.categories %}
							{{ loop.last ? categorie.title : categorie.title~' - ' }}
						{% endfor %}
					</td>
					<td>
						{{project.title}}
					</td>
					<td class="text-center">
						{{project.creationDate|date('d-m-Y')}}
					</td>
					<td class="text-center">
						{{project.startDate|date('d-m-Y')}}
					</td>
					<td class="text-center">
						{{project.endDate|date('d-m-Y')}}
					</td>
					<td class="text-center">
						{% if project.status == 0 %}
							<a href=""><span class="glyphicon glyphicon-check text-success"></span></a>
							<a href=""><span class="glyphicon glyphicon-remove-circle text-danger"></span></a>
							{% elseif project.status > 1 and project.status <= 3 %}
							<span class="glyphicon glyphicon-check text-muted"></span>
							<span class="glyphicon glyphicon-remove-circle text-muted"></span>
						{% elseif project.status >= 4 %}
							<span class="glyphicon glyphicon-ok text-success"></span>
							<a href=""><span class="glyphicon glyphicon-remove-circle text-danger"></span></a>
						{% elseif project.status == 1 %}
						<a href=""><span class="glyphicon glyphicon-check text-success"></span></a>
							<a href=""><span class="glyphicon glyphicon-remove text-danger"></span></a>
						{% endif %}	
					</td>
				</tr>
			{% endfor %}
		</table>
	</div>
{% endblock content %}

